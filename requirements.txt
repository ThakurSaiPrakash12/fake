# Production Runtime Dependencies - CPU Only
# NOTE: torch and torchvision installed separately in Dockerfile with CPU index
# Do NOT add them here - they're installed via https://download.pytorch.org/whl/cpu

# Core Flask (Web Framework)
Flask==3.0.2
Werkzeug==3.0.1
gunicorn==23.0.0

# Flask Extensions
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
python-dotenv==1.0.0

# Image/Video Processing (headless = no GUI dependencies)
opencv-python-headless==4.9.0.80
Pillow==10.2.0

# Database
SQLAlchemy==2.0.25

# NumPy (dependency for torch/torchvision, installed separately)